import { TranslatePassPipe } from './translate-pass.pipe';

describe('TranslatePassPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatePassPipe();
    expect(pipe).toBeTruthy();
  });
});
